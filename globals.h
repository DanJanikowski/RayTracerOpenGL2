#pragma once

// Header file storing global constants and variables
namespace globals {
	const unsigned int WINDOW_WIDTH = 1300, WINDOW_HEIGHT = 1300;
	const double HALF_WW = WINDOW_WIDTH / 2.0, HALF_WH = WINDOW_HEIGHT / 2.0;
}